name: Deploy via FTP

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build
        
      - name: Deploy via SFTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: dj9eew.ftp.infomaniak.com
          username: dj9eew_system
          password: x5VLK-7_@3llB
          local-dir: ./dist/portfolio/
          server-dir: /sites/thomas-ambroise.fr/
          protocol: ftps
          security: strict